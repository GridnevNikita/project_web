# Общая информация
Проект представляет собой набор модулей для обработки финансовых данных, включающий функции для маскировки номеров карт и счетов, фильтрации и сортировки операций, а также форматирования дат.

## Структура проекта

### Модуль masks
Содержит функции для маскировки конфиденциальной информации:

* **get_mask_card_number** - маскирует номер карты (16 цифр)
* **get_mask_account** - маскирует номер счета (20 цифр)

Пример маскировки карты:
1234 56** **** 7890


Пример маскировки счета:
**7890


### Модуль widget
Содержит функции для обработки данных:

* **mask_account_card** - маскирует номера карт и счетов в строковом представлении
* **get_date** - преобразует дату из формата ISO в формат DD.MM.YYYY

### Модуль processing
Предоставляет инструменты для работы с данными операций:

* **filter_by_state** - фильтрация операций по состоянию (по умолчанию 'EXECUTED')
* **sort_by_date** - сортировка операций по дате (по убыванию по умолчанию)

### Модуль generators
Предоставляет инструменты для генерации и обработки данных:

#### **card_number_generator** - генератор номеров банковских карт в формате XXXX XXXX XXXX XXXX

* Генерирует последовательность номеров банковских карт
Форматирует номера в стандартный вид XXXX XXXX XXXX XXXX
* Поддерживает генерацию диапазонов номеров
* Проверяет корректность входных значений

Функции фильтрации операций
#### **filter_by_currency** - фильтрация операций по валюте

* Работает с вложенными структурами данных
* Возвращает итератор с отфильтрованными операциями
* Поддерживает различные валюты

#### **transaction_descriptions** - извлекает описания транзакций из списка операций

* Обрабатывает отсутствие описания в операции
* Возвращает итератор с описаниями
* Гарантирует наличие хотя бы базового описания для каждой операции

### Модуль data_loader
Содержит функции для загрузки транзакций из файлов:

* **csv_transactions** — загружает данные из CSV-файла с разделителем `;` и кодировкой `utf-8`
* **excel_transactions** — загружает данные из Excel-файлов формата `.xlsx`

Примеры:

```python
from src.data_loader import csv_transactions, excel_transactions

transactions = csv_transactions("../data/transactions.csv")
transactions = excel_transactions("../data/transactions_excel.xlsx")
```

## Tests

### Модуль test_masks 

* Тестирование правильности маскирования номера карты.
* Проверка работы функции на различных входных форматах номеров карт, включая граничные случаи и нестандартные длины номеров.
* Проверка, что функция корректно обрабатывает входные строки, где отсутствует номер карты.
* Тестирование правильности маскирования номера счета.
* Проверка работы функции с различными форматами и длинами номеров счетов.
* Проверка, что функция корректно обрабатывает входные данные, где номер счета меньше ожидаемой длины.

### Модуль test_widget
* Тесты для проверки, что функция корректно распознает и применяет нужный тип маскировки в зависимости от типа входных данных (карта или счет).
* Параметризованные тесты с разными типами карт и счетов для проверки универсальности функции.
* Тестирование функции на обработку некорректных входных данных и проверка ее устойчивости к ошибкам.
* Тестирование правильности преобразования даты.
* Проверка работы функции на различных входных форматах даты, включая граничные случаи и нестандартные строки с датами.
* Проверка, что функция корректно обрабатывает входные строки, где отсутствует дата.
### Модуль test_processing
* Тестирование фильтрации списка словарей по заданному статусу `state`.
* Проверка работы функции при отсутствии словарей с указанным статусом `state` в списке.
* Параметризация тестов для различных возможных значений статуса`state`.
* Тестирование сортировки списка словарей по датам в порядке убывания и возрастания.
* Проверка корректности сортировки при одинаковых датах.
* Тесты на работу функции с некорректными или нестандартными форматами дат.

### Модуль test_generators
#### filter_by_currency
* Проверка фильтрации по USD.
* Проверка фильтрации по несуществующей валюте.
* Тестирование с пустым списком.
* Проверка количества отфильтрованных транзакций.
#### transaction_descriptions
* Тестирование валидных описаний.
* Проверка обработки транзакций без описания.
* Тестирование с пустым списком
#### card_number_generator
* Проверка базовой генерации последовательности
* Тестирование форматирования номеров
* Проверка обработки крайних значений
### Модуль test_data_loader
* Проверка загрузки данных из CSV и Excel файлов.
* Проверка реакции на отсутствие файлов (`FileNotFoundError`).
* Проверка обработки ошибок чтения файлов (`ValueError`).
* Использование `unittest.mock.patch` для подмены `os.path.isfile`, `pandas.read_csv` и `pandas.read_excel`.
* Проверка, что функции вызываются с ожидаемыми аргументами (`assert_called_once_with`).